{{#defineBlock "Module1"}}

  {{#defineBlockContainer}}
    {{!--
      {{settings}} is a helper that would write out options as attributes,
      so output here might look something like:

      <div class="Module1" data-bsp-setting-foo="bar" data-bsp-setting-bar="foo">
    --}}
    <div class="{{blockName}}" {{settings}}>
    {{#defineBlockBody}}
      {{!-- content --}}
    {{/defineBlockBody}}
    </div>
  {{/defineBlockContainer}}

{{/defineBlock}}
